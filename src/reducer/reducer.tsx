const initialState = {
    loading: false,
    user: null,
    listBullet : []
}
let newState : any = {...initialState};
export function reducer(state: any = initialState, action: any) {
    switch (action.type) {
        
        default:
            return state
    }
}